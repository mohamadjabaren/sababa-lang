# ==========================================
# ספריית מתמטיקה (Math Library)
# ==========================================

פונקציה חיבור(x, y) {
    תחזיר x + y
}

פונקציה כפול(x, y) {
    תחזיר x * y
}

פונקציה ריבוע(x) {
    תחזיר x * x
}