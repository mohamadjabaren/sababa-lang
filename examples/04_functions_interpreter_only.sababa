# Interpreter only. The C back end does not implement FunctionDef yet,
# so compiler.py will report an error on this file. Run it from the IDE.
יאללה

פונקציה עצרת(n) {
    אם n <= 1 אז {
        תחזיר 1
    } אחרת {
        תחזיר n * עצרת(n - 1)
    }
}

תדפיס עצרת(5)

סיימנו
